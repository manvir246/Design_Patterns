Where?
when we need to create object on the basis of certain condition, then we need to factory pattern.
otherwise we have to write if else, I mean if this then give that object if <condition> then other object, so code is duplicated so much.l